export const TransformerOptions = {
  // if a class property has a default value, that value will be exposed in the transformed object, even if it’s not part of the input
  exposeDefaultValues: true,
};
